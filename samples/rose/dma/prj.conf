CONFIG_ROSE=y
CONFIG_PRINTK=y
# Drop the RISC-V exception stack-trace helper: on small images its GP-relative
# reference to z_interrupt_stacks (.noinit) overflows the +/-2KB GP window
# (R_RISCV_GPREL_I relocation truncated). Not needed for these tests.
CONFIG_EXCEPTION_STACK_TRACE=n
