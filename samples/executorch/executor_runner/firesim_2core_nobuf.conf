# 2-core FireSim overlay, unbuffered HTIF, for a clean (no-profiling) 2-core
# measurement to compare against the 1-core clean run.
CONFIG_MP_MAX_NUM_CPUS=2
CONFIG_MAIN_STACK_SIZE=524288
CONFIG_UART_HTIF_SYSCALL_PRINT=y
CONFIG_UART_HTIF_BUFFERED_OUTPUT=n
