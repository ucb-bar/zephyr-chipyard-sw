sample:
  description: Generated DNN inference harness (modelblaster flow)
  name: modelblaster_harness
common:
  tags: modelblaster dnn
  integration_platforms:
    - spike_riscv64
  harness: console
  harness_config:
    type: multi_line
    regex:
      - "=== MODELBLASTER_OUTPUT_BEGIN ==="
      - "=== MODELBLASTER_OUTPUT_END ==="
tests:
  sample.modelblaster.harness:
    tags: modelblaster
