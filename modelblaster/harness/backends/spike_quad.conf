# Overlay for spike runs targeting -p4 (default). Pairs with
# `spike --isa=... -p4` invoked by modelblaster/validation/spike_runner.
#
# Lives separately from harness_xpurt/prj.conf so the per-target hart
# count is in one place — see the matching note at the top of prj.conf.
CONFIG_MP_MAX_NUM_CPUS=4
