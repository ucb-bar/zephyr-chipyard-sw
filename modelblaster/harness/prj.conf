# 4 MB main stack — large enough for im2col-style algorithms that allocate
# OH*OW * IC*KH*KW floats as a stack VLA. DroNet's worst case is about
# 295 KB; we leave plenty of headroom for larger models.
CONFIG_MAIN_STACK_SIZE=4194304
CONFIG_CBPRINTF_FP_SUPPORT=y
CONFIG_FPU=y
