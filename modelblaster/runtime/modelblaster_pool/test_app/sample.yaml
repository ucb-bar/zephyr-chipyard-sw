sample:
  description: modelblaster_pool unit test (raw-pthread parallel-for)
  name: modelblaster_pool_test_app
common:
  tags: modelblaster runtime modelblaster_pool
  integration_platforms:
    - spike_riscv64
  harness: console
  harness_config:
    type: multi_line
    regex:
      - "=== MODELBLASTER_POOL_TEST_BEGIN ==="
      - "=== MODELBLASTER_POOL_TEST_END ==="
tests:
  sample.modelblaster.runtime.modelblaster_pool:
    tags: modelblaster
