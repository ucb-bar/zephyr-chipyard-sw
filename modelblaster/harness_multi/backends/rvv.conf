CONFIG_RISCV_ISA_EXT_V=y
# Eager V context switching — lazy-trap routing on secondary harts has a
# regression where the first vector instruction on hart!=0 takes an
# illegal-instruction trap that doesn't reach z_riscv_fpu_trap.
CONFIG_RISCV_ISA_EXT_V_LAZY=n
