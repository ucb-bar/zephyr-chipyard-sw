sample:
  description: Cross-hart threadpool dispatch microbenchmarks
  name: agents_microbench_threadpool
common:
  tags: agents microbench threadpool
  integration_platforms:
    - spike_riscv64
  harness: console
  harness_config:
    type: multi_line
    regex:
      - "=== THREADPOOL_BENCH_BEGIN ==="
tests:
  sample.agents.microbench_threadpool:
    tags: agents
