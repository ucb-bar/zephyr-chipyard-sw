# gemmini_q31 backend, harness_multi variant. Same Kconfig delta as the
# single-model gemmini_q31 overlay (i.e. none); kept as a sibling file
# so the per-backend overlay lookup pattern works for TARGET=gemmini_q31
# multi-model builds.
