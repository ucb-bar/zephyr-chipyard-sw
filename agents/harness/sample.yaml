sample:
  description: Generated DNN inference harness (agents flow)
  name: agents_harness
common:
  tags: agents dnn
  integration_platforms:
    - spike_riscv64
  harness: console
  harness_config:
    type: multi_line
    regex:
      - "=== AGENTS_OUTPUT_BEGIN ==="
      - "=== AGENTS_OUTPUT_END ==="
tests:
  sample.agents.harness:
    tags: agents
